# config/app.yaml
environment:
  current: "development" 

server:
  port: "8080"
  cookiesecure: false
  read_timeout: 15
  write_timeout: 15
  idle_timeout: 60

database:
  path: "postgres://username:password@localhost/dbname?sslmode=disable"
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "REDACTED"
  dbname: "horizon"
  
database_connections:
  max_open_conns: 25
  max_idle_conns: 10
  max_life_time: 5
  max_idle_time: 2

redis:
  addr: "localhost:6379"
  password: "REDACTED"
  db: 0

jwt:
  secretkey: "ultra_super_strong_secret_key_XFJ12JTPM"

ratelimit:
  maxrequests: 100
  window: 1m 

email:
  smtHost: "smtp.gmail.com"
  smtPort: "587"
  username: "REDACTED"
  password: "REDACTED"
  from: "Horizon Messenger <soldathekler@gmail.com>"

tracing:
  enabled: true
  service_name: "massager"
  endpoint: "http://localhost:14268/api/traces"